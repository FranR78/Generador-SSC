Notas técnicas ya fusionadas (consolidadas en una sola ficha).

Estas fichas NO las lee `construir.py` (solo mira `notas/`). Se guardan por
trazabilidad: cada nota fusionada en `notas/` lista sus NT de origen en
`fusionadas:` y en su apartado «Notas fusionadas».

No borrar: son la fuente de cada consolidación por si hay que revisarla.
